{{ if eq .Type "like" }}
  <article class="like">
    <h1>
      liked
      {{ range .Posts }}
        <span class="h-entry">
          <a class="u-like-of" href="{{ get . "like-of.properties.url" }}">{{ if has . "like-of.properties.name" }}{{ get . "like-of.properties.name" }}{{ else }}{{ get . "like-of.properties.url" }}{{ end }}</a>
          <a class="u-url" href="{{ get . "url" }}">at
            <time class="dt-published" datetime="{{ get . "published" }}">
              {{ time . "published" }}
            </time>
          </a>
        </span>
      {{ end }}
    </h1>
    {{ template "entry_meta.gotmpl" .Meta }}
  </article>
{{ else }}
  {{ template "entry.gotmpl" .Meta }}
{{ end }}

